node_modules
dist
.DS_Store
*.local

# BrowserStack harness — secrets + generated artifacts
browserstack/.env
browserstack/node_modules
browserstack/screenshots
browserstack/report.json
browserstack/report.md
browserstack/*.log

# Orb bake harness — raw recordings (intermediate; encoded clips live in public/orb/)
bake-recordings/

# Sailing trailer — raw iMovie master(s) (intermediate; web clips live in public/trailer/)
trailer-src/

# Stray macOS screenshots dropped into public/ — never deploy these
/public/Screenshot*.png
